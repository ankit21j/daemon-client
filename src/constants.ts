export const PATH_VARIABLES = {
  buffer: "data/buffer",
  delivery: "data/delivery",
  pickup: "data/Laser",
  reporting: "data/Laser/LaserLog"
}

export const POLLING_INTERVALS = {
  config_watcher: 1 * 60 * 1000,
  sku_store_watcher: 4 * 60 * 1000
}
